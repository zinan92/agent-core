# Skills

This package ships two kinds of skills:

- `agent-onboarding/` = the core bootstrap mechanism
- `default/` = the baseline skills every new agent should have

Add company-shared and agent-specific skills only after the baseline layer is in place.
